export interface Professor {
  id: number;
  name: string;
}
export interface Course {
  id: number;
  name: string;
  number: string;
}

export interface Topic {
  id: number;
  name: string;
  description: string;
}